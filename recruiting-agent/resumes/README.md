# Local resume folder (not committed)

Place candidate PDFs, DOCX, or TXT files here for **local testing only**.

This directory is **gitignored** — real resumes must never be pushed to GitHub.

## Quick test

```bash
# From project root, with venv active and Groq key in Settings (or groq_key.txt locally):
python scripts/reimport_resumes.py
```

Or upload via the web UI: **Upload** page.
